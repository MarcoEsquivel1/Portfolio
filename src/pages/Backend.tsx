import animatedTransition from '../animation/animatedTransition';
export const Backend = animatedTransition(() => {
    return(
        <div id="backend" className="container">
            <h1>Backend</h1>
        </div>
    );
})