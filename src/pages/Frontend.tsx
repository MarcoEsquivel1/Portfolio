/* eslint-disable react-refresh/only-export-components */
import animatedTransition from '../animation/animatedTransition';
export const Frontend = animatedTransition(() => {
    return(
        <div id="frontend" className="container">
            <h1>Frontend</h1>
        </div>
    );
})